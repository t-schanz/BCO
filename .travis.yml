language: python

matrix:
    include:
#        - os: linux
#          sudo: false
#          python: 3.6
#          env: TOXENV=py36
#        - os: linux
#          sudo: false
#          python: 3.5
#          env: TOXENV=py35
#        - os: linux
#          sudo: false
#          python: 2.7
#          env: TOXENV=py27
        - os: osx
          language: generic
          env: TOXENV=py36


before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then wget https://repo.continuum.io/miniconda/Miniconda3-3.6.0-Linux-x86_64.sh -O ~/miniconda.sh; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then bash ~/miniconda.sh -b -p $HOME/miniconda; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then export PATH="$HOME/miniconda/bin:$PATH"; fi

install:
  - python --version
  - python setup.py install

script:
  - python BCO/_tests/runAll.py